# Core HTTP client
requests>=2.28.0

# yfinance — primary price feed for OHLCV history, fundamentals, dividends, earnings
yfinance>=0.2.28
pandas>=2.0.0

# AI-powered fallback scraper (ScrapeGraphAI)
scrapegraphai>=2.1.0